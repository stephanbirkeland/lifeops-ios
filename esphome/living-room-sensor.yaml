# Living Room Multi-Sensor
# ESP32-C3 Super Mini + BME280 + AM312 PIR
# Purpose: Comfort monitoring, presence detection

substitutions:
  device_name: living-room-sensor
  friendly_name: "Living Room Sensor"

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf

packages:
  common: !include common.yaml

# Deep sleep for battery optimization
deep_sleep:
  id: deep_sleep_control
  run_duration: 30s
  sleep_duration: 5min

i2c:
  sda: GPIO6
  scl: GPIO7
  scan: true

sensor:
  - platform: bme280_i2c
    temperature:
      name: "Living Room Temperature"
      id: living_room_temp
      oversampling: 2x
    humidity:
      name: "Living Room Humidity"
      id: living_room_humidity
      oversampling: 2x
    pressure:
      name: "Living Room Pressure"
    address: 0x76
    update_interval: 60s

  - platform: adc
    pin: GPIO0
    name: "Living Room Sensor Battery"
    id: battery_voltage
    update_interval: 60s
    attenuation: 11db
    filters:
      - multiply: 2.0
    unit_of_measurement: "V"

  - platform: template
    name: "Living Room Sensor Battery Percent"
    unit_of_measurement: "%"
    accuracy_decimals: 0
    lambda: |-
      float voltage = id(battery_voltage).state;
      float percent = (voltage - 3.0) / (4.2 - 3.0) * 100;
      if (percent > 100) percent = 100;
      if (percent < 0) percent = 0;
      return percent;
    update_interval: 60s

binary_sensor:
  - platform: gpio
    pin:
      number: GPIO4
      mode: INPUT_PULLDOWN
    name: "Living Room Motion"
    id: living_room_motion
    device_class: motion
    filters:
      - delayed_off: 60s  # Longer delay for living room
    on_press:
      then:
        - deep_sleep.prevent: deep_sleep_control
    on_release:
      then:
        - delay: 120s
        - deep_sleep.allow: deep_sleep_control
